You are working pebble asp-5a8 on branch worker/asp-5a8.

Title: ASP-FSM-REDUNDANT (warn): two case/branch bodies over one enum with identical normalized AST — likely redundant states

original_statement:
  Request: Origin: exophial exo-011 (2026-07-18), rule 4 of the sourced design; assigned to aspergillus but never filed there (filed 2026-07-22).
  THE RULE: within one match statement (or if/elif chain) dispatching on an Enum-typed subject, two branch bodies whose NORMALIZED AST is identical → WARN 'likely redundant states'.
  MOTIVATING DEFECT: would have flagged exophial's PRESERVE/FAIL redundancy (both branches were `Fail(reason)`) — the hand-found malformed-FSM instance that motivated exo-011.
  WARN, NOT REJECT — stated plainly per exo-011: textual-identical-body is a decidable PROXY for state redundancy; true behavioral equivalence is undecidable (Rice). Expect false negatives; never promise semantic redundancy detection.
  NORMALIZATION: strip locations/comments; do not alpha-rename (keep it a conservative, explainable proxy).
  ACCEPTANCE: fixture with two enum cases having AST-identical bodies → WARNED; bodies differing only in comments/whitespace → still WARNED; genuinely different bodies → silent. Non-enum match subjects out of scope.

criticality: negligible

goal: Add a WARN-level static check to aspergillus that flags two branch bodies of a single Enum-typed match/if-elif dispatch when their location- and comment-stripped (but not alpha-renamed) ASTs are identical, emitting 'likely redundant states' — catching the exact PRESERVE/FAIL-style redundant-branch defect that motivated exo-011, while staying silent on non-enum dispatch, genuinely different bodies, and bodies that differ only by variable naming.

restatement:
  This request closes the last unfiled piece of exophial's exo-011 design ('Enforce well-formed FSMs statically', 2026-07-18): rule 4, which was assigned to aspergillus but never actually filed here until 2026-07-22. The rule requires aspergillus's static checker to gain a new WARN-level check: within a single match statement or if/elif chain that dispatches on one Enum-typed subject, if two of its branch bodies have identical normalized ASTs, the checker must emit a warning reading 'likely redundant states'. Normalization here means stripping source locations and comments before comparing — it explicitly does NOT rename variables to a canonical form, so two branches that differ only by variable name are still treated as different and stay silent; this keeps the check a conservative, explainable, textual proxy rather than a claim about real program behavior. This rule exists because textual-identical-body is a decidable stand-in for a genuinely undecidable question (true behavioral equivalence between two branches, per Rice's theorem) — the checker WARNS rather than REJECTS precisely because it will have false negatives and must never be read as proving two states are semantically redundant. The concrete motivating case is real: exophial had a PRESERVE/FAIL branch pair that were both literally `Fail(reason)` — a hand-found malformed-FSM defect this rule would have caught automatically. The rule only applies when the dispatch subject is Enum-typed; dispatch over non-enum subjects (e.g. plain strings or ints) is out of scope even if their branch bodies happen to be identical. The deliverable is one self-contained static check, demonstrated against a fixture collecting several dispatch scenarios, each producing one specific, independently observable warning-count (and, for the core case, exact warning text) when the checker is run over it.

restatement_spans:
- core-rule: within one match statement (or if/elif chain) dispatching on an Enum-typed subject, two branch bodies whose NORMALIZED AST is identical → WARN 'likely redundant states'
- normalization: strip locations/comments; do not alpha-rename (keep it a conservative, explainable proxy)
- comments-whitespace-still-warned: bodies differing only in comments/whitespace → still WARNED
- genuinely-different-silent: genuinely different bodies → silent
- non-enum-out-of-scope: Non-enum match subjects out of scope

acceptance_criteria:
- core-rule: Show the checker two branches of an enum dispatch that do exactly the same thing (like the real PRESERVE/FAIL bug, where both branches just returned Fail(reason)). The checker should flag exactly one warning, worded exactly 'likely redundant states'.
- comments-whitespace-still-warned: Show the checker two branches of an enum dispatch where one branch has an extra comment and different indentation/whitespace, but otherwise does the same thing as the other branch. The checker should still flag exactly one warning.
- normalization: Show the checker two branches of an enum dispatch that use a different local variable name but are otherwise structured the same way. Because the rule deliberately never renames variables to compare bodies, this should NOT be flagged.
- genuinely-different-silent: Show the checker two branches of an enum dispatch that genuinely do different things. The checker should report no warnings at all.
- non-enum-out-of-scope: Show the checker a dispatch (match or if/elif) over a plain non-enum value (like a string or number) where two branches happen to do exactly the same thing. Since the subject isn't an Enum, the checker should report no warnings.

NOTE (appended):
spec: contracts/specs/derived-asp-5a8.spec.json

NOTE (appended):
base-sha: f7d5d890f2c096347ace1733391d5dd621811d1f

OUTER LOOP — this pebble is linked to a spec. It is your acceptance
oracle: the reducer re-runs this exact spec as the merge gate, so make
these pass and nothing else decides merge. Work outside-in against it.

Linked spec (repo-relative, the SAME file the gate runs): contracts/specs/derived-asp-5a8.spec.json
Acceptance tests you must satisfy:
     1. {'type': 'behavior', 'description': "Show the checker two branches of an enum dispatch that do exactly the same thing (like the real PRESERVE/FAIL bug, where both branches just returned Fail(reason)). The checker should flag exactly one warning, worded exactly 'likely redundant states'.", 'proves': 'Proves the core rule: identical-normalized-AST branches over an Enum-typed dispatch are warned, with the exact required wording.', 'restatement_span_id': 'core-rule', 'when': {'interpreter': 'python3', 'script': 'scripts/check_asp_fsm_redundant_branches.py', 'args': ['tests/fixtures/asp_fsm_redundant.py', 'redundant_identical_bodies'], 'stdin': ''}, 'then': [{'observe': 'stdout_json', 'field': 'warning_count', 'op': 'eq', 'value': 1}, {'observe': 'stdout_json', 'field': 'first_message', 'op': 'eq', 'value': 'likely redundant states'}], 'max_runs': 1, 'timeout_seconds': 30, 'clause_id': 'c1'}
     2. {'type': 'behavior', 'description': 'Show the checker two branches of an enum dispatch where one branch has an extra comment and different indentation/whitespace, but otherwise does the same thing as the other branch. The checker should still flag exactly one warning.', 'proves': "Proves the normalization step strips comments and whitespace/location noise before comparing, so cosmetic differences alone don't hide a real redundant pair.", 'restatement_span_id': 'comments-whitespace-still-warned', 'when': {'interpreter': 'python3', 'script': 'scripts/check_asp_fsm_redundant_branches.py', 'args': ['tests/fixtures/asp_fsm_redundant.py', 'redundant_comment_whitespace_diff'], 'stdin': ''}, 'then': [{'observe': 'stdout_json', 'field': 'warning_count', 'op': 'eq', 'value': 1}], 'max_runs': 1, 'timeout_seconds': 30, 'clause_id': 'c2'}
     3. {'type': 'behavior', 'description': 'Show the checker two branches of an enum dispatch that use a different local variable name but are otherwise structured the same way. Because the rule deliberately never renames variables to compare bodies, this should NOT be flagged.', 'proves': "Proves the 'do not alpha-rename' design rule holds: a variable-name-only difference makes the normalized ASTs unequal, so the pair stays silent rather than being force-matched.", 'restatement_span_id': 'normalization', 'when': {'interpreter': 'python3', 'script': 'scripts/check_asp_fsm_redundant_branches.py', 'args': ['tests/fixtures/asp_fsm_redundant.py', 'renamed_variable_not_flagged'], 'stdin': ''}, 'then': [{'observe': 'stdout_json', 'field': 'warning_count', 'op': 'eq', 'value': 0}], 'max_runs': 1, 'timeout_seconds': 30, 'clause_id': 'c3'}
     4. {'type': 'behavior', 'description': 'Show the checker two branches of an enum dispatch that genuinely do different things. The checker should report no warnings at all.', 'proves': "Proves the check doesn't false-positive on branches that are actually different — it only fires on true AST-identical bodies.", 'restatement_span_id': 'genuinely-different-silent', 'when': {'interpreter': 'python3', 'script': 'scripts/check_asp_fsm_redundant_branches.py', 'args': ['tests/fixtures/asp_fsm_redundant.py', 'genuinely_different_bodies'], 'stdin': ''}, 'then': [{'observe': 'stdout_json', 'field': 'warning_count', 'op': 'eq', 'value': 0}], 'max_runs': 1, 'timeout_seconds': 30, 'clause_id': 'c4'}
     5. {'type': 'behavior', 'description': "Show the checker a dispatch (match or if/elif) over a plain non-enum value (like a string or number) where two branches happen to do exactly the same thing. Since the subject isn't an Enum, the checker should report no warnings.", 'proves': "Proves the rule stays scoped to Enum-typed dispatch subjects and doesn't fire on non-enum dispatch even when branch bodies happen to match.", 'restatement_span_id': 'non-enum-out-of-scope', 'when': {'interpreter': 'python3', 'script': 'scripts/check_asp_fsm_redundant_branches.py', 'args': ['tests/fixtures/asp_fsm_redundant.py', 'non_enum_dispatch_identical_bodies'], 'stdin': ''}, 'then': [{'observe': 'stdout_json', 'field': 'warning_count', 'op': 'eq', 'value': 0}], 'max_runs': 1, 'timeout_seconds': 30, 'clause_id': 'c5'}

When the task is done, complete it through the typed-handoff contract (do NOT open a pull request — the exophial reducer integrates your branch itself once the handoff is valid):

1. Commit ALL your work to this branch (the reducer merges from it).
2. Write a YAML handoff to .pebbles/handoffs/asp-5a8.yaml that validates against the
   exophial done.schema.json. For a successful task it MUST contain:
     - status: completed
     - pebble_id: asp-5a8
     - summary: one paragraph on what you did and how you verified it
     - cost: your token spend for this task (e.g. "~40k tokens")
     - commits: the list of commit SHAs you made on this branch. Write
       each SHA as a QUOTED string (e.g. "6983640"), never a bare
       scalar — YAML parses an all-digit bare SHA as an integer, which
       fails schema validation.
     - verify_transcript: a mapping of verb label -> stdout tail for
       every verify command you ran (e.g. pytest, mypy, ruff)
     - EXACTLY ONE of e2e_gate OR e2e_exempt (never both, never
       neither):
         * If a REAL no-mock outer-loop test proves this change, use
           e2e_gate with ALL these fields and no others: spec, command, result, ran_at_sha, mocks_in_path.
           result MUST be the string "passed"; mocks_in_path MUST be
           the boolean false; ran_at_sha is the branch commit you ran
           the test at, written as a QUOTED string (e.g. "6983640"),
           never a bare scalar — YAML parses an all-digit bare SHA as
           an integer, which fails schema validation; spec is the
           test's repo-relative path; command is the exact command you
           ran.
         * Otherwise (no real outer-loop test — e.g. a trivial,
           infrastructure, docs, or refactor change), use e2e_exempt
           with {category, reason}. category MUST be exactly one of:
           docs, refactor, infrastructure, pebble-filing, hygiene, outer-loop-undrivable. reason must be at least 12 characters.
   If you could NOT finish, instead write status: blocked (with
   blocker + next_action) or status: failed (with failure_diagnosis);
   pebble_id, summary, and cost are always required.

The gate watches .pebbles/handoffs/asp-5a8.yaml; if you do not write it, the task times out and nothing merges.
