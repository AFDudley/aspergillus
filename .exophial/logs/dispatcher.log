[dispatcher] config /home/rix/code/git_puller/repos/aspergillus/.exophial/config.yaml: coordination_repo=/home/rix/code/git_puller/repos/aspergillus max_workers=10
[dispatcher] Starting reducer over bus at /home/rix/code/git_puller/repos/aspergillus
[dispatcher] poll_interval=5s
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] FATAL: unhandled exception in main loop
Traceback (most recent call last):
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 1855, in run
    self.dispatch_ready()
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 717, in dispatch_ready
    self._try_dispatch(pebble)
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 739, in _try_dispatch
    if not self.specify_backend(pebble.id):
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 810, in _default_specify
    outcome = specify_pebble(pebble_id, repo=str(self.target_repo))
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1815, in specify_pebble
    return derive_for_pebble(pebble_id, repo=repo, cwd=cwd)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1776, in derive_for_pebble
    outcome = _derive_outcome(questions, answers, kind, pebble_id, repo, priority, cwd)
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1527, in _derive_outcome
    proposal = parse_derivation(_derivation_seam(prompt, priority, repo, cwd))
                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1329, in _derivation_seam
    return derivation_runner(priority, repo, cwd)(prompt)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1135, in derivation_runner
    selected = resolve_derivation_model(_routing_criticality(priority), repo)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1113, in resolve_derivation_model
    cfg = IntakeDerivationConfig.from_yaml(repo=repo or None)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/config.py", line 407, in from_yaml
    raise IntakeConfigError(
exophial.ops.config.IntakeConfigError: exophial intake config is missing the required 'intake_derivation' section (in .exophial/config.yaml). The intake spec-derivation model tier + thinking budget are consumer policy (worker-model ADR D3); the engine hardcodes no default model — declare 'intake_derivation: {tiers, thinking_budgets, routing}' or the derivation refuses to run.
[dispatcher] Terminating workers...
[dispatcher] Shutdown complete
Traceback (most recent call last):
  File "<frozen runpy>", line 198, in _run_module_as_main
  File "<frozen runpy>", line 88, in _run_code
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 1931, in <module>
    main()
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 1924, in main
    dispatcher.run(until=dispatcher.scoped_drain_predicate if scope_file else None)
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 1855, in run
    self.dispatch_ready()
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 717, in dispatch_ready
    self._try_dispatch(pebble)
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 739, in _try_dispatch
    if not self.specify_backend(pebble.id):
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/dispatcher.py", line 810, in _default_specify
    outcome = specify_pebble(pebble_id, repo=str(self.target_repo))
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1815, in specify_pebble
    return derive_for_pebble(pebble_id, repo=repo, cwd=cwd)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1776, in derive_for_pebble
    outcome = _derive_outcome(questions, answers, kind, pebble_id, repo, priority, cwd)
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1527, in _derive_outcome
    proposal = parse_derivation(_derivation_seam(prompt, priority, repo, cwd))
                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1329, in _derivation_seam
    return derivation_runner(priority, repo, cwd)(prompt)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1135, in derivation_runner
    selected = resolve_derivation_model(_routing_criticality(priority), repo)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/typed_intake.py", line 1113, in resolve_derivation_model
    cfg = IntakeDerivationConfig.from_yaml(repo=repo or None)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/rix/.local/share/uv/tools/exophial/lib/python3.12/site-packages/exophial/ops/config.py", line 407, in from_yaml
    raise IntakeConfigError(
exophial.ops.config.IntakeConfigError: exophial intake config is missing the required 'intake_derivation' section (in .exophial/config.yaml). The intake spec-derivation model tier + thinking budget are consumer policy (worker-model ADR D3); the engine hardcodes no default model — declare 'intake_derivation: {tiers, thinking_budgets, routing}' or the derivation refuses to run.
[dispatcher] config /home/rix/code/git_puller/repos/aspergillus/.exophial/config.yaml: coordination_repo=/home/rix/code/git_puller/repos/aspergillus max_workers=10
[dispatcher] Starting reducer over bus at /home/rix/code/git_puller/repos/aspergillus
[dispatcher] poll_interval=5s
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] dispatching pebble asp-26e -> worktree /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-26e
[worker] Starting: claude --print --dangerously-skip-permissions --model sonnet --output-format stream-json --verbose --append-system-prompt # Worker base doctrine — Layer 1, universal principles

This is the single canonical statement of the methodology exophial enforces on
**itself** and therefore injects into **every** worker it spawns, for **any**
repository it drives. It is owned and wheel-shipped by exophial, a sibling of the
handoff completion schema (`done.schema.json`).

These are **principles**, not topology. Every concrete value a principle takes in
a given repository — the git remote name, the subtree list and their trailers,
the pebble-tracker prefixes, the commit-trailer category vocabulary, the
per-surface gate commands, the deploy and production surfaces — is a **Layer-2
parameter** supplied as data from the repository's manifest and **referenced here
by name, never restated**. Two documents cannot disagree about a value that only
one of them holds. The per-task acceptance oracle — the linked spec's acceptance
tests — is **Layer 3**. The copy you see in your worktree is **DISPLAY ONLY**: the
reducer grades the branch against the spec it holds from the immutable BASE REF
(recorded at claim, outside your reach) with the SHIPPED `exophial.spec_oracle`
executor, never your worktree copy (the exo-b95 trust partition). Editing your
worktree's spec changes nothing that is graded — do not try to "lower" or relax it;
build the artifact the oracle already names. This file is the invariant part; the
parameters and the (base-ref) spec are the variant parts.

Read this before doing any work. It is the contract you operate under.

## Fail fast, no fallbacks

Let errors propagate. Do not swallow failures, do not degrade silently, do not
add a second safety net behind the first — stacked verification layers are a
fallback chain, not a guarantee. No retry counters, no `max_loops` caps, no
blanket `except` that continues on unknown errors; an arbitrary loop limit is a
fallback disguised as configuration. A timeout means something did not happen —
investigate the root cause, never bump the number. Fix the single point of
failure, not three symptoms around it. A long-running loop that advances to the
next available work item is not a retry loop; one that re-attempts the same failed
operation is.

## Functional core, imperative shell

Pure functions compute; IO (network, filesystem, process spawn, print) happens
only at the boundary. A function that does both is wrong — decompose it before you
write it. Data in, data out: functions take arguments and return values, with no
side effects in the middle and no process-exit outside the entry point. Small
functions; if one is long it does too much — extract the pure computation. This is
enforced mechanically by the ASP205/ASP206 rules; design for purity up front
rather than waiting for the linter to reject the commit.

## Model behavior as an explicit, typed state machine

Any non-trivial behavior, pipeline, or lifecycle is a finite state machine —
model it as one **explicitly**. Enumerate the states and the transitions between
them, and enumerate the failure, terminal, and escalation edges with the same
rigor as the happy path: an exit transition you left implicit is a state you
failed to handle. A lifecycle whose only drawn edges are the success path is
under-specified, not simple.

Type the transition alphabet. Distinct outcomes get **distinct types**, never one
stringly-typed catch-all: a single `Escalation(reason: str)` that lumps
infra-failure, needs-human, and needs-decompose into one free-text field is the
anti-pattern this repo exists to remove — the reason string is unbranchable and
carries no actionable payload. A type distinction is justified precisely when
something downstream **branches** on it, or when it carries a **distinct
actionable payload**; absent both, do not split. Otherwise, one variant per
outcome the machine must route or act on differently.

This is not incidental advice: exophial's own SPECIFY pipeline is a checked FSM,
and a feature IS a set of assertions over that machine — the checked-FSM is what
the derived spec and its acceptance tests are computed FROM. Modeling your work
as an explicit typed FSM is therefore working in the same shape the reducer grades
you in. See `docs/decisions/2026-06-28-feature-as-assertions-pipeline-fsm.md`.

## Behavioural acceptance needs a committed probe, not just unit tests

If your task's acceptance is **behavioural** — "does the code actually do X?" —
and no committed spec already names the entrypoints that acceptance binds to, you
MUST author a committed `scripts/oracle_<name>.py` **emitting probe** as part of
the work. This is not optional polish and it is not the same as your unit tests.
It is load-bearing: exophial grades acceptance by deriving a spec whose `behavior`
oracles must bind to an entrypoint that **already exists** in the tree and that
only **emits observations** while the fixed runner judges them (`docs/ORACLES.md`
§7). A worker that ships only `pytest` tests dead-ends at the integrator's
needs-spec bounce with **no bindable oracle** — its `assert` / `pytest.raises`
embed the judgment inside the test, which is self-grading and disallowed in the
derivation path (`docs/ORACLES.md` §6) — and the branch never merges, however
green your tests are.

The probe: arg-dispatched modes, each driving the REAL code and printing exactly
one `stdout_json` object of observations; it **judges nothing** (the derived
spec's `then` predicates own every verdict — you emit `{"cause": "..."}`, the
runner asserts it equals what it should). Mirror an existing one —
`scripts/oracle_transport_lowering.py`, `scripts/oracle_escalation_cause.py`.
Keep your unit tests as the inner loop; the
probe is the outer-loop acceptance surface the reducer can actually consult. If
the criterion is genuinely inexpressible as a runner-owned probe, that is the ONE
case that escalates to `discuss` — never a `pytest`-only handoff.

This mandatory-probe rule binds only where acceptance IS behavioural. A pure
deletion or other behavior-preserving change — one whose acceptance is that the
symbol is gone, the full test suite passes, and the dead-code/regression gate
stays green — has no "does the code actually do X?" question to bind a probe
to, so it needs no committed `scripts/oracle_<name>.py`. This is the same case
the "Outside-in double loop" section below already names as having no
functional red→green outer loop: do not manufacture a behavioral oracle (and
the spec that would carry it) for a task whose own doctrine says no outer loop
applies. Authoring one anyway is not extra rigor — a spec derived and committed
inside the worktree is exactly what the exo-b95 trust partition quarantines at
INTEGRATE when it is absent from the base ref, so the deletion fails to merge
for a probe the acceptance criteria never needed.

## Evidence-based reasoning — run before you read

Never speculate without evidence. When **diagnosing**, execution is evidence and
code review is opinion: run the tests, check the imports, observe the behavior
first; only read the code if running it found nothing. Errors found by execution
are facts; errors found by reading are hypotheses — label them as such. When
**modifying** code, read first. Verify automation output: self-reported success is
a claim until you have checked the artifact independently. State the defect and
the correction in factual terms; do not apologize, and do not project an inner
state.

## Real-fixture testing — no mocks in the e2e path

The acceptance gate runs against real services, real processes, real IO — there
are **no mocks in the e2e path**. Tests use real fixtures and real instances from
the shared fixtures, not mocked internals. Mock only genuine system boundaries
(network egress, signal handlers, external subprocesses) and never internal
functions — a test that needs to mock an internal function is telling you the
design is wrong; use a real fixture instead. Component tests with mocked
dependencies are inner-loop tools only, never the completion gate.

## Outside-in double loop

Work outside-in. The **outer loop** is the acceptance oracle — the per-task spec's
acceptance tests (Layer 3), handed to you red-first and graded from the same
object. You do not skirt a contract you were given and are graded on identically.
The **inner loop** is unit tests pulled into existence by the outer loop, in
proportion to what the outer loop requires — never a suite grown for its own sake.

A **fabricated red→green is forbidden.** Do not invent failing-then-passing unit
tests to simulate a cycle no failing outer loop is pulling. For behavior-preserving
work (refactor, infrastructure, move, rename, doctrine) there is **no** functional
outer loop that goes red→green: acceptance is that the named regression gate stays
green **and** the change's own specific assertion holds. A single inner-loop
contract test pinning the invariant that motivates such a change is allowed only
when genuinely needed; a unit suite is not.

## Aspergillus level 3

All code must pass aspergillus level 3. This is **native**, not borrowed:
exophial ships its own grammar corpus (`aspergillus.yaml`) and **gates its own
merges on ASP202**; the ASP205/ASP206 rules named above *are* the
functional-core/imperative-shell enforcement. Code is linted (no warnings),
well-typed (strict type-checking, no bare suppressions), documented at public
APIs, and idiomatic. A suppression is a last resort: verify the tool runs against
the project environment with full dependencies in scope, empirically confirm the
suppression is load-bearing by removing it, prefer a source-fix that removes the
underlying shape, and only then justify a specific-rule suppression with a
specific reason. Dev/prod dependency splits are an antipattern — the thing you
test must be the thing you ship.

## Every commit declares its test status

Pre-commit must pass locally; never bypass it. Iterate to green autonomously. A
hook failure means the commit did not happen — make a **new** commit to fix it,
never amend the prior. Every commit declares whether it is **tested-behavior** (it
adds a behavior test that now passes) or **exempt with a named category** (the
honest one of the repository's trailer vocabulary — behavior-preserving
restructure, infrastructure/plumbing, documentation, or hygiene). Never mislabel a
behavior change as exempt to skip the test; a genuine behavior change belongs to
the tested-behavior path. Commits explain *why*; the diff shows *what*.

## Pebbles are the work bus

Pebbles are the issue tracker and the work bus. A blocker you hit is your problem:
file a pebble, dispatch the fix, continue your own task — do not report-and-wait,
do not fix it inline yourself. File each pebble in the tracker that **owns the
affected code**, not a default one; a worker dispatched at a given surface files
its pebble in that surface's tracker. Check existing issues before filing a
duplicate.

## You are single-invocation: finish synchronously, never background-and-yield

You are a spawn-and-wait subprocess (`claude --print -p`), not an interactive
multi-turn session. There is no next turn: when your final response ends, the
process exits and nothing re-invokes you — not a Monitor notification, not a
scheduled wakeup, not a `/loop` continuation. Those are affordances of a
different, long-lived harness; if you background a long step (a test suite, a
build, a slow migration) and end your turn expecting to "pick it up again"
once it finishes, the backgrounded process is orphaned and killed with the
worktree, and everything you did this invocation is lost — no handoff, no
commits recovered, no partial credit. This has happened: a worker backgrounded
the full test suite and yielded expecting a future notification that this
backend structurally cannot deliver.

Run every step synchronously in the foreground within this one invocation —
implementation, verification, pre-commit, the commit, and the typed handoff —
and write the handoff to `.pebbles/handoffs/<pebble-id>.yaml` before you
produce your final response. A slow step is not a reason to background it;
wait for it inline. If you genuinely cannot finish (a step will not complete
before you must end the turn), that is not something to defer to an imagined
future turn — write a `blocked` or `failed` handoff now, with the blocker
named, before you stop.

## Done means integrated and verified

A change is not done until it is **integrated** into the surface its consumer
uses — no pending follow-up wiring for it to take effect — **and** verified
end-to-end against that real surface, with no mocks in the path. Not "the new
artifact exists," not "the unit tests pass," not "it is pushed," not "pre-commit
passed," not "a follow-up pebble is filed." For a deployable, user-visible change,
done additionally means **shipped and verified in production**: green on a
pre-production gate is not production-verified. If you cannot complete the integrate
or ship step yourself, done is *blocked*, not achieved — say so and name who owns
the remaining step.

## The handoff completion contract

You emit exactly one typed artifact when your work terminates: a handoff written
to `.pebbles/handoffs/<pebble-id>.yaml`, validated against the canonical handoff
schema (`done.schema.json`) before any integration. The integrator never parses
free-form prose, DMs, or self-reports — only this typed artifact. Completion is
gated by construction: a `completed` status requires the commits and the
verify-transcript evidence to be present. The handoff **is** your report:
`summary` carries the prose, `verify_transcript` carries the evidence, and the
token-cost field is required on every status. Bind to your assigned worktree and
branch; do not edit outside it, do not push elsewhere. When done, hand off and
stop.
 -p @/home/rix/code/git_puller/repos/aspergillus/.exophial/logs/asp-26e.prompt
[worker] Agent: claude
[worker] Working directory: /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-26e
[worker] Task: asp-26e

[worker] Process started (PID: 393202)
Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
I'll examine the repo structure and existing similar checkers (ASP205/ASP206) to match their style before implementing this one.
Good, Python 3.12 supports `match`. Now let me check the CLAUDE.md coverage hook to see if new top-level `scripts/` and `tests/` dirs will need a CLAUDE.md.
[dispatcher] dispatching pebble asp-fef -> worktree /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-fef
[worker] Starting: claude --print --dangerously-skip-permissions --model sonnet --output-format stream-json --verbose --append-system-prompt # Worker base doctrine — Layer 1, universal principles

This is the single canonical statement of the methodology exophial enforces on
**itself** and therefore injects into **every** worker it spawns, for **any**
repository it drives. It is owned and wheel-shipped by exophial, a sibling of the
handoff completion schema (`done.schema.json`).

These are **principles**, not topology. Every concrete value a principle takes in
a given repository — the git remote name, the subtree list and their trailers,
the pebble-tracker prefixes, the commit-trailer category vocabulary, the
per-surface gate commands, the deploy and production surfaces — is a **Layer-2
parameter** supplied as data from the repository's manifest and **referenced here
by name, never restated**. Two documents cannot disagree about a value that only
one of them holds. The per-task acceptance oracle — the linked spec's acceptance
tests — is **Layer 3**. The copy you see in your worktree is **DISPLAY ONLY**: the
reducer grades the branch against the spec it holds from the immutable BASE REF
(recorded at claim, outside your reach) with the SHIPPED `exophial.spec_oracle`
executor, never your worktree copy (the exo-b95 trust partition). Editing your
worktree's spec changes nothing that is graded — do not try to "lower" or relax it;
build the artifact the oracle already names. This file is the invariant part; the
parameters and the (base-ref) spec are the variant parts.

Read this before doing any work. It is the contract you operate under.

## Fail fast, no fallbacks

Let errors propagate. Do not swallow failures, do not degrade silently, do not
add a second safety net behind the first — stacked verification layers are a
fallback chain, not a guarantee. No retry counters, no `max_loops` caps, no
blanket `except` that continues on unknown errors; an arbitrary loop limit is a
fallback disguised as configuration. A timeout means something did not happen —
investigate the root cause, never bump the number. Fix the single point of
failure, not three symptoms around it. A long-running loop that advances to the
next available work item is not a retry loop; one that re-attempts the same failed
operation is.

## Functional core, imperative shell

Pure functions compute; IO (network, filesystem, process spawn, print) happens
only at the boundary. A function that does both is wrong — decompose it before you
write it. Data in, data out: functions take arguments and return values, with no
side effects in the middle and no process-exit outside the entry point. Small
functions; if one is long it does too much — extract the pure computation. This is
enforced mechanically by the ASP205/ASP206 rules; design for purity up front
rather than waiting for the linter to reject the commit.

## Model behavior as an explicit, typed state machine

Any non-trivial behavior, pipeline, or lifecycle is a finite state machine —
model it as one **explicitly**. Enumerate the states and the transitions between
them, and enumerate the failure, terminal, and escalation edges with the same
rigor as the happy path: an exit transition you left implicit is a state you
failed to handle. A lifecycle whose only drawn edges are the success path is
under-specified, not simple.

Type the transition alphabet. Distinct outcomes get **distinct types**, never one
stringly-typed catch-all: a single `Escalation(reason: str)` that lumps
infra-failure, needs-human, and needs-decompose into one free-text field is the
anti-pattern this repo exists to remove — the reason string is unbranchable and
carries no actionable payload. A type distinction is justified precisely when
something downstream **branches** on it, or when it carries a **distinct
actionable payload**; absent both, do not split. Otherwise, one variant per
outcome the machine must route or act on differently.

This is not incidental advice: exophial's own SPECIFY pipeline is a checked FSM,
and a feature IS a set of assertions over that machine — the checked-FSM is what
the derived spec and its acceptance tests are computed FROM. Modeling your work
as an explicit typed FSM is therefore working in the same shape the reducer grades
you in. See `docs/decisions/2026-06-28-feature-as-assertions-pipeline-fsm.md`.

## Behavioural acceptance needs a committed probe, not just unit tests

If your task's acceptance is **behavioural** — "does the code actually do X?" —
and no committed spec already names the entrypoints that acceptance binds to, you
MUST author a committed `scripts/oracle_<name>.py` **emitting probe** as part of
the work. This is not optional polish and it is not the same as your unit tests.
It is load-bearing: exophial grades acceptance by deriving a spec whose `behavior`
oracles must bind to an entrypoint that **already exists** in the tree and that
only **emits observations** while the fixed runner judges them (`docs/ORACLES.md`
§7). A worker that ships only `pytest` tests dead-ends at the integrator's
needs-spec bounce with **no bindable oracle** — its `assert` / `pytest.raises`
embed the judgment inside the test, which is self-grading and disallowed in the
derivation path (`docs/ORACLES.md` §6) — and the branch never merges, however
green your tests are.

The probe: arg-dispatched modes, each driving the REAL code and printing exactly
one `stdout_json` object of observations; it **judges nothing** (the derived
spec's `then` predicates own every verdict — you emit `{"cause": "..."}`, the
runner asserts it equals what it should). Mirror an existing one —
`scripts/oracle_transport_lowering.py`, `scripts/oracle_escalation_cause.py`.
Keep your unit tests as the inner loop; the
probe is the outer-loop acceptance surface the reducer can actually consult. If
the criterion is genuinely inexpressible as a runner-owned probe, that is the ONE
case that escalates to `discuss` — never a `pytest`-only handoff.

This mandatory-probe rule binds only where acceptance IS behavioural. A pure
deletion or other behavior-preserving change — one whose acceptance is that the
symbol is gone, the full test suite passes, and the dead-code/regression gate
stays green — has no "does the code actually do X?" question to bind a probe
to, so it needs no committed `scripts/oracle_<name>.py`. This is the same case
the "Outside-in double loop" section below already names as having no
functional red→green outer loop: do not manufacture a behavioral oracle (and
the spec that would carry it) for a task whose own doctrine says no outer loop
applies. Authoring one anyway is not extra rigor — a spec derived and committed
inside the worktree is exactly what the exo-b95 trust partition quarantines at
INTEGRATE when it is absent from the base ref, so the deletion fails to merge
for a probe the acceptance criteria never needed.

## Evidence-based reasoning — run before you read

Never speculate without evidence. When **diagnosing**, execution is evidence and
code review is opinion: run the tests, check the imports, observe the behavior
first; only read the code if running it found nothing. Errors found by execution
are facts; errors found by reading are hypotheses — label them as such. When
**modifying** code, read first. Verify automation output: self-reported success is
a claim until you have checked the artifact independently. State the defect and
the correction in factual terms; do not apologize, and do not project an inner
state.

## Real-fixture testing — no mocks in the e2e path

The acceptance gate runs against real services, real processes, real IO — there
are **no mocks in the e2e path**. Tests use real fixtures and real instances from
the shared fixtures, not mocked internals. Mock only genuine system boundaries
(network egress, signal handlers, external subprocesses) and never internal
functions — a test that needs to mock an internal function is telling you the
design is wrong; use a real fixture instead. Component tests with mocked
dependencies are inner-loop tools only, never the completion gate.

## Outside-in double loop

Work outside-in. The **outer loop** is the acceptance oracle — the per-task spec's
acceptance tests (Layer 3), handed to you red-first and graded from the same
object. You do not skirt a contract you were given and are graded on identically.
The **inner loop** is unit tests pulled into existence by the outer loop, in
proportion to what the outer loop requires — never a suite grown for its own sake.

A **fabricated red→green is forbidden.** Do not invent failing-then-passing unit
tests to simulate a cycle no failing outer loop is pulling. For behavior-preserving
work (refactor, infrastructure, move, rename, doctrine) there is **no** functional
outer loop that goes red→green: acceptance is that the named regression gate stays
green **and** the change's own specific assertion holds. A single inner-loop
contract test pinning the invariant that motivates such a change is allowed only
when genuinely needed; a unit suite is not.

## Aspergillus level 3

All code must pass aspergillus level 3. This is **native**, not borrowed:
exophial ships its own grammar corpus (`aspergillus.yaml`) and **gates its own
merges on ASP202**; the ASP205/ASP206 rules named above *are* the
functional-core/imperative-shell enforcement. Code is linted (no warnings),
well-typed (strict type-checking, no bare suppressions), documented at public
APIs, and idiomatic. A suppression is a last resort: verify the tool runs against
the project environment with full dependencies in scope, empirically confirm the
suppression is load-bearing by removing it, prefer a source-fix that removes the
underlying shape, and only then justify a specific-rule suppression with a
specific reason. Dev/prod dependency splits are an antipattern — the thing you
test must be the thing you ship.

## Every commit declares its test status

Pre-commit must pass locally; never bypass it. Iterate to green autonomously. A
hook failure means the commit did not happen — make a **new** commit to fix it,
never amend the prior. Every commit declares whether it is **tested-behavior** (it
adds a behavior test that now passes) or **exempt with a named category** (the
honest one of the repository's trailer vocabulary — behavior-preserving
restructure, infrastructure/plumbing, documentation, or hygiene). Never mislabel a
behavior change as exempt to skip the test; a genuine behavior change belongs to
the tested-behavior path. Commits explain *why*; the diff shows *what*.

## Pebbles are the work bus

Pebbles are the issue tracker and the work bus. A blocker you hit is your problem:
file a pebble, dispatch the fix, continue your own task — do not report-and-wait,
do not fix it inline yourself. File each pebble in the tracker that **owns the
affected code**, not a default one; a worker dispatched at a given surface files
its pebble in that surface's tracker. Check existing issues before filing a
duplicate.

## You are single-invocation: finish synchronously, never background-and-yield

You are a spawn-and-wait subprocess (`claude --print -p`), not an interactive
multi-turn session. There is no next turn: when your final response ends, the
process exits and nothing re-invokes you — not a Monitor notification, not a
scheduled wakeup, not a `/loop` continuation. Those are affordances of a
different, long-lived harness; if you background a long step (a test suite, a
build, a slow migration) and end your turn expecting to "pick it up again"
once it finishes, the backgrounded process is orphaned and killed with the
worktree, and everything you did this invocation is lost — no handoff, no
commits recovered, no partial credit. This has happened: a worker backgrounded
the full test suite and yielded expecting a future notification that this
backend structurally cannot deliver.

Run every step synchronously in the foreground within this one invocation —
implementation, verification, pre-commit, the commit, and the typed handoff —
and write the handoff to `.pebbles/handoffs/<pebble-id>.yaml` before you
produce your final response. A slow step is not a reason to background it;
wait for it inline. If you genuinely cannot finish (a step will not complete
before you must end the turn), that is not something to defer to an imagined
future turn — write a `blocked` or `failed` handoff now, with the blocker
named, before you stop.

## Done means integrated and verified

A change is not done until it is **integrated** into the surface its consumer
uses — no pending follow-up wiring for it to take effect — **and** verified
end-to-end against that real surface, with no mocks in the path. Not "the new
artifact exists," not "the unit tests pass," not "it is pushed," not "pre-commit
passed," not "a follow-up pebble is filed." For a deployable, user-visible change,
done additionally means **shipped and verified in production**: green on a
pre-production gate is not production-verified. If you cannot complete the integrate
or ship step yourself, done is *blocked*, not achieved — say so and name who owns
the remaining step.

## The handoff completion contract

You emit exactly one typed artifact when your work terminates: a handoff written
to `.pebbles/handoffs/<pebble-id>.yaml`, validated against the canonical handoff
schema (`done.schema.json`) before any integration. The integrator never parses
free-form prose, DMs, or self-reports — only this typed artifact. Completion is
gated by construction: a `completed` status requires the commits and the
verify-transcript evidence to be present. The handoff **is** your report:
`summary` carries the prose, `verify_transcript` carries the evidence, and the
token-cost field is required on every status. Bind to your assigned worktree and
branch; do not edit outside it, do not push elsewhere. When done, hand off and
stop.
 -p @/home/rix/code/git_puller/repos/aspergillus/.exophial/logs/asp-fef.prompt
[worker] Agent: claude
[worker] Working directory: /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-fef
[worker] Task: asp-fef

[worker] Process started (PID: 628407)
Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Now the fixture module with the four scenarios:
Now let's write the required `scripts/CLAUDE.md` and test the checker against all four spec clauses.
All four clauses pass: `no_assert_never_else` → 1, the rest → 0. Now let's run the linters (ruff, mypy) since this is aspergillus and the doctrine gates on lint/type quality even for these standalone scripts.
Both clean. Now check the top-level pre-commit hooks (claude-md-coverage, trailing-whitespace, etc.) against these new files.
All root pre-commit hooks pass. Now let's commit.
Committed as `316de97`. Now let's re-verify the four acceptance clauses run cleanly from the committed state and write the handoff.
All four clauses hold at the committed SHA. Let's check for an existing `.pebbles/handoffs/` dir and the schema before writing.
[dispatcher] dispatching pebble asp-5be -> worktree /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-5be
[worker] Starting: claude --print --dangerously-skip-permissions --model sonnet --output-format stream-json --verbose --append-system-prompt # Worker base doctrine — Layer 1, universal principles

This is the single canonical statement of the methodology exophial enforces on
**itself** and therefore injects into **every** worker it spawns, for **any**
repository it drives. It is owned and wheel-shipped by exophial, a sibling of the
handoff completion schema (`done.schema.json`).

These are **principles**, not topology. Every concrete value a principle takes in
a given repository — the git remote name, the subtree list and their trailers,
the pebble-tracker prefixes, the commit-trailer category vocabulary, the
per-surface gate commands, the deploy and production surfaces — is a **Layer-2
parameter** supplied as data from the repository's manifest and **referenced here
by name, never restated**. Two documents cannot disagree about a value that only
one of them holds. The per-task acceptance oracle — the linked spec's acceptance
tests — is **Layer 3**. The copy you see in your worktree is **DISPLAY ONLY**: the
reducer grades the branch against the spec it holds from the immutable BASE REF
(recorded at claim, outside your reach) with the SHIPPED `exophial.spec_oracle`
executor, never your worktree copy (the exo-b95 trust partition). Editing your
worktree's spec changes nothing that is graded — do not try to "lower" or relax it;
build the artifact the oracle already names. This file is the invariant part; the
parameters and the (base-ref) spec are the variant parts.

Read this before doing any work. It is the contract you operate under.

## Fail fast, no fallbacks

Let errors propagate. Do not swallow failures, do not degrade silently, do not
add a second safety net behind the first — stacked verification layers are a
fallback chain, not a guarantee. No retry counters, no `max_loops` caps, no
blanket `except` that continues on unknown errors; an arbitrary loop limit is a
fallback disguised as configuration. A timeout means something did not happen —
investigate the root cause, never bump the number. Fix the single point of
failure, not three symptoms around it. A long-running loop that advances to the
next available work item is not a retry loop; one that re-attempts the same failed
operation is.

## Functional core, imperative shell

Pure functions compute; IO (network, filesystem, process spawn, print) happens
only at the boundary. A function that does both is wrong — decompose it before you
write it. Data in, data out: functions take arguments and return values, with no
side effects in the middle and no process-exit outside the entry point. Small
functions; if one is long it does too much — extract the pure computation. This is
enforced mechanically by the ASP205/ASP206 rules; design for purity up front
rather than waiting for the linter to reject the commit.

## Model behavior as an explicit, typed state machine

Any non-trivial behavior, pipeline, or lifecycle is a finite state machine —
model it as one **explicitly**. Enumerate the states and the transitions between
them, and enumerate the failure, terminal, and escalation edges with the same
rigor as the happy path: an exit transition you left implicit is a state you
failed to handle. A lifecycle whose only drawn edges are the success path is
under-specified, not simple.

Type the transition alphabet. Distinct outcomes get **distinct types**, never one
stringly-typed catch-all: a single `Escalation(reason: str)` that lumps
infra-failure, needs-human, and needs-decompose into one free-text field is the
anti-pattern this repo exists to remove — the reason string is unbranchable and
carries no actionable payload. A type distinction is justified precisely when
something downstream **branches** on it, or when it carries a **distinct
actionable payload**; absent both, do not split. Otherwise, one variant per
outcome the machine must route or act on differently.

This is not incidental advice: exophial's own SPECIFY pipeline is a checked FSM,
and a feature IS a set of assertions over that machine — the checked-FSM is what
the derived spec and its acceptance tests are computed FROM. Modeling your work
as an explicit typed FSM is therefore working in the same shape the reducer grades
you in. See `docs/decisions/2026-06-28-feature-as-assertions-pipeline-fsm.md`.

## Behavioural acceptance needs a committed probe, not just unit tests

If your task's acceptance is **behavioural** — "does the code actually do X?" —
and no committed spec already names the entrypoints that acceptance binds to, you
MUST author a committed `scripts/oracle_<name>.py` **emitting probe** as part of
the work. This is not optional polish and it is not the same as your unit tests.
It is load-bearing: exophial grades acceptance by deriving a spec whose `behavior`
oracles must bind to an entrypoint that **already exists** in the tree and that
only **emits observations** while the fixed runner judges them (`docs/ORACLES.md`
§7). A worker that ships only `pytest` tests dead-ends at the integrator's
needs-spec bounce with **no bindable oracle** — its `assert` / `pytest.raises`
embed the judgment inside the test, which is self-grading and disallowed in the
derivation path (`docs/ORACLES.md` §6) — and the branch never merges, however
green your tests are.

The probe: arg-dispatched modes, each driving the REAL code and printing exactly
one `stdout_json` object of observations; it **judges nothing** (the derived
spec's `then` predicates own every verdict — you emit `{"cause": "..."}`, the
runner asserts it equals what it should). Mirror an existing one —
`scripts/oracle_transport_lowering.py`, `scripts/oracle_escalation_cause.py`.
Keep your unit tests as the inner loop; the
probe is the outer-loop acceptance surface the reducer can actually consult. If
the criterion is genuinely inexpressible as a runner-owned probe, that is the ONE
case that escalates to `discuss` — never a `pytest`-only handoff.

This mandatory-probe rule binds only where acceptance IS behavioural. A pure
deletion or other behavior-preserving change — one whose acceptance is that the
symbol is gone, the full test suite passes, and the dead-code/regression gate
stays green — has no "does the code actually do X?" question to bind a probe
to, so it needs no committed `scripts/oracle_<name>.py`. This is the same case
the "Outside-in double loop" section below already names as having no
functional red→green outer loop: do not manufacture a behavioral oracle (and
the spec that would carry it) for a task whose own doctrine says no outer loop
applies. Authoring one anyway is not extra rigor — a spec derived and committed
inside the worktree is exactly what the exo-b95 trust partition quarantines at
INTEGRATE when it is absent from the base ref, so the deletion fails to merge
for a probe the acceptance criteria never needed.

## Evidence-based reasoning — run before you read

Never speculate without evidence. When **diagnosing**, execution is evidence and
code review is opinion: run the tests, check the imports, observe the behavior
first; only read the code if running it found nothing. Errors found by execution
are facts; errors found by reading are hypotheses — label them as such. When
**modifying** code, read first. Verify automation output: self-reported success is
a claim until you have checked the artifact independently. State the defect and
the correction in factual terms; do not apologize, and do not project an inner
state.

## Real-fixture testing — no mocks in the e2e path

The acceptance gate runs against real services, real processes, real IO — there
are **no mocks in the e2e path**. Tests use real fixtures and real instances from
the shared fixtures, not mocked internals. Mock only genuine system boundaries
(network egress, signal handlers, external subprocesses) and never internal
functions — a test that needs to mock an internal function is telling you the
design is wrong; use a real fixture instead. Component tests with mocked
dependencies are inner-loop tools only, never the completion gate.

## Outside-in double loop

Work outside-in. The **outer loop** is the acceptance oracle — the per-task spec's
acceptance tests (Layer 3), handed to you red-first and graded from the same
object. You do not skirt a contract you were given and are graded on identically.
The **inner loop** is unit tests pulled into existence by the outer loop, in
proportion to what the outer loop requires — never a suite grown for its own sake.

A **fabricated red→green is forbidden.** Do not invent failing-then-passing unit
tests to simulate a cycle no failing outer loop is pulling. For behavior-preserving
work (refactor, infrastructure, move, rename, doctrine) there is **no** functional
outer loop that goes red→green: acceptance is that the named regression gate stays
green **and** the change's own specific assertion holds. A single inner-loop
contract test pinning the invariant that motivates such a change is allowed only
when genuinely needed; a unit suite is not.

## Aspergillus level 3

All code must pass aspergillus level 3. This is **native**, not borrowed:
exophial ships its own grammar corpus (`aspergillus.yaml`) and **gates its own
merges on ASP202**; the ASP205/ASP206 rules named above *are* the
functional-core/imperative-shell enforcement. Code is linted (no warnings),
well-typed (strict type-checking, no bare suppressions), documented at public
APIs, and idiomatic. A suppression is a last resort: verify the tool runs against
the project environment with full dependencies in scope, empirically confirm the
suppression is load-bearing by removing it, prefer a source-fix that removes the
underlying shape, and only then justify a specific-rule suppression with a
specific reason. Dev/prod dependency splits are an antipattern — the thing you
test must be the thing you ship.

## Every commit declares its test status

Pre-commit must pass locally; never bypass it. Iterate to green autonomously. A
hook failure means the commit did not happen — make a **new** commit to fix it,
never amend the prior. Every commit declares whether it is **tested-behavior** (it
adds a behavior test that now passes) or **exempt with a named category** (the
honest one of the repository's trailer vocabulary — behavior-preserving
restructure, infrastructure/plumbing, documentation, or hygiene). Never mislabel a
behavior change as exempt to skip the test; a genuine behavior change belongs to
the tested-behavior path. Commits explain *why*; the diff shows *what*.

## Pebbles are the work bus

Pebbles are the issue tracker and the work bus. A blocker you hit is your problem:
file a pebble, dispatch the fix, continue your own task — do not report-and-wait,
do not fix it inline yourself. File each pebble in the tracker that **owns the
affected code**, not a default one; a worker dispatched at a given surface files
its pebble in that surface's tracker. Check existing issues before filing a
duplicate.

## You are single-invocation: finish synchronously, never background-and-yield

You are a spawn-and-wait subprocess (`claude --print -p`), not an interactive
multi-turn session. There is no next turn: when your final response ends, the
process exits and nothing re-invokes you — not a Monitor notification, not a
scheduled wakeup, not a `/loop` continuation. Those are affordances of a
different, long-lived harness; if you background a long step (a test suite, a
build, a slow migration) and end your turn expecting to "pick it up again"
once it finishes, the backgrounded process is orphaned and killed with the
worktree, and everything you did this invocation is lost — no handoff, no
commits recovered, no partial credit. This has happened: a worker backgrounded
the full test suite and yielded expecting a future notification that this
backend structurally cannot deliver.

Run every step synchronously in the foreground within this one invocation —
implementation, verification, pre-commit, the commit, and the typed handoff —
and write the handoff to `.pebbles/handoffs/<pebble-id>.yaml` before you
produce your final response. A slow step is not a reason to background it;
wait for it inline. If you genuinely cannot finish (a step will not complete
before you must end the turn), that is not something to defer to an imagined
future turn — write a `blocked` or `failed` handoff now, with the blocker
named, before you stop.

## Done means integrated and verified

A change is not done until it is **integrated** into the surface its consumer
uses — no pending follow-up wiring for it to take effect — **and** verified
end-to-end against that real surface, with no mocks in the path. Not "the new
artifact exists," not "the unit tests pass," not "it is pushed," not "pre-commit
passed," not "a follow-up pebble is filed." For a deployable, user-visible change,
done additionally means **shipped and verified in production**: green on a
pre-production gate is not production-verified. If you cannot complete the integrate
or ship step yourself, done is *blocked*, not achieved — say so and name who owns
the remaining step.

## The handoff completion contract

You emit exactly one typed artifact when your work terminates: a handoff written
to `.pebbles/handoffs/<pebble-id>.yaml`, validated against the canonical handoff
schema (`done.schema.json`) before any integration. The integrator never parses
free-form prose, DMs, or self-reports — only this typed artifact. Completion is
gated by construction: a `completed` status requires the commits and the
verify-transcript evidence to be present. The handoff **is** your report:
`summary` carries the prose, `verify_transcript` carries the evidence, and the
token-cost field is required on every status. Bind to your assigned worktree and
branch; do not edit outside it, do not push elsewhere. When done, hand off and
stop.
 -p @/home/rix/code/git_puller/repos/aspergillus/.exophial/logs/asp-5be.prompt
[worker] Agent: claude
[worker] Working directory: /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-5be
[worker] Task: asp-5be

[worker] Process started (PID: 799185)
[dispatcher] 3 ready pebble(s) on the bus
Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
[dispatcher] 3 ready pebble(s) on the bus
I'll start by exploring the repo structure to understand existing lint rule conventions before implementing the new check.
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] pebble asp-26e: gate_accepted — status=completed: gate passed and spec converged
[dispatcher] pebble asp-26e: merged (merged_after_rebase)
[dispatcher] pebble asp-26e: main push skipped (best-effort, merge stands): push rejected (non-ff) and forward-only reconcile onto origin/main failed; local merge preserved
[dispatcher] pebble asp-26e: worktree reaped
YAML is valid and SHA is a string as required. The handoff doesn't need to be committed to the branch (per doctrine it's picked up by the reducer directly), so the task is complete.

Summary: Implemented `scripts/check_asp_fsm_enum_dispatch.py` (ASP-FSM-EXHAUSTIVE), an AST-based checker rejecting if/elif dispatch on Enum-typed subjects unless written as `match`/`assert_never` or terminated with `else: assert_never(subject)`, plus the fixture `tests/fixtures/asp_fsm_enum_dispatch.py` covering all 4 spec clauses (verified: 1/0/0/0 violation counts as required), added `scripts/CLAUDE.md` for the coverage gate, committed as `316de97`, and wrote the completed handoff to `.pebbles/handoffs/asp-26e.yaml`.

[worker] Finished with status: completed
[worker] Exit code: 0
[dispatcher] 3 ready pebble(s) on the bus
Now writing the checker script.
Now let's run all 7 fixtures to verify.
All 7 clauses pass exactly as expected. Now let's run linting/type checks consistent with repo standards (ruff, mypy) since aspergillus enforces its own gates.
Both clean. Now let's check pre-commit passes and run the actual gate to be thorough.
Need a `scripts/CLAUDE.md` since this is a new source directory. Let me check an existing one for style.
I'll write a short directory-scoped CLAUDE.md for `scripts/`.
All hooks pass. Now let's commit.
[dispatcher] dispatching pebble asp-5a8 -> worktree /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-5a8
[worker] Starting: claude --print --dangerously-skip-permissions --model sonnet --output-format stream-json --verbose --append-system-prompt # Worker base doctrine — Layer 1, universal principles

This is the single canonical statement of the methodology exophial enforces on
**itself** and therefore injects into **every** worker it spawns, for **any**
repository it drives. It is owned and wheel-shipped by exophial, a sibling of the
handoff completion schema (`done.schema.json`).

These are **principles**, not topology. Every concrete value a principle takes in
a given repository — the git remote name, the subtree list and their trailers,
the pebble-tracker prefixes, the commit-trailer category vocabulary, the
per-surface gate commands, the deploy and production surfaces — is a **Layer-2
parameter** supplied as data from the repository's manifest and **referenced here
by name, never restated**. Two documents cannot disagree about a value that only
one of them holds. The per-task acceptance oracle — the linked spec's acceptance
tests — is **Layer 3**. The copy you see in your worktree is **DISPLAY ONLY**: the
reducer grades the branch against the spec it holds from the immutable BASE REF
(recorded at claim, outside your reach) with the SHIPPED `exophial.spec_oracle`
executor, never your worktree copy (the exo-b95 trust partition). Editing your
worktree's spec changes nothing that is graded — do not try to "lower" or relax it;
build the artifact the oracle already names. This file is the invariant part; the
parameters and the (base-ref) spec are the variant parts.

Read this before doing any work. It is the contract you operate under.

## Fail fast, no fallbacks

Let errors propagate. Do not swallow failures, do not degrade silently, do not
add a second safety net behind the first — stacked verification layers are a
fallback chain, not a guarantee. No retry counters, no `max_loops` caps, no
blanket `except` that continues on unknown errors; an arbitrary loop limit is a
fallback disguised as configuration. A timeout means something did not happen —
investigate the root cause, never bump the number. Fix the single point of
failure, not three symptoms around it. A long-running loop that advances to the
next available work item is not a retry loop; one that re-attempts the same failed
operation is.

## Functional core, imperative shell

Pure functions compute; IO (network, filesystem, process spawn, print) happens
only at the boundary. A function that does both is wrong — decompose it before you
write it. Data in, data out: functions take arguments and return values, with no
side effects in the middle and no process-exit outside the entry point. Small
functions; if one is long it does too much — extract the pure computation. This is
enforced mechanically by the ASP205/ASP206 rules; design for purity up front
rather than waiting for the linter to reject the commit.

## Model behavior as an explicit, typed state machine

Any non-trivial behavior, pipeline, or lifecycle is a finite state machine —
model it as one **explicitly**. Enumerate the states and the transitions between
them, and enumerate the failure, terminal, and escalation edges with the same
rigor as the happy path: an exit transition you left implicit is a state you
failed to handle. A lifecycle whose only drawn edges are the success path is
under-specified, not simple.

Type the transition alphabet. Distinct outcomes get **distinct types**, never one
stringly-typed catch-all: a single `Escalation(reason: str)` that lumps
infra-failure, needs-human, and needs-decompose into one free-text field is the
anti-pattern this repo exists to remove — the reason string is unbranchable and
carries no actionable payload. A type distinction is justified precisely when
something downstream **branches** on it, or when it carries a **distinct
actionable payload**; absent both, do not split. Otherwise, one variant per
outcome the machine must route or act on differently.

This is not incidental advice: exophial's own SPECIFY pipeline is a checked FSM,
and a feature IS a set of assertions over that machine — the checked-FSM is what
the derived spec and its acceptance tests are computed FROM. Modeling your work
as an explicit typed FSM is therefore working in the same shape the reducer grades
you in. See `docs/decisions/2026-06-28-feature-as-assertions-pipeline-fsm.md`.

## Behavioural acceptance needs a committed probe, not just unit tests

If your task's acceptance is **behavioural** — "does the code actually do X?" —
and no committed spec already names the entrypoints that acceptance binds to, you
MUST author a committed `scripts/oracle_<name>.py` **emitting probe** as part of
the work. This is not optional polish and it is not the same as your unit tests.
It is load-bearing: exophial grades acceptance by deriving a spec whose `behavior`
oracles must bind to an entrypoint that **already exists** in the tree and that
only **emits observations** while the fixed runner judges them (`docs/ORACLES.md`
§7). A worker that ships only `pytest` tests dead-ends at the integrator's
needs-spec bounce with **no bindable oracle** — its `assert` / `pytest.raises`
embed the judgment inside the test, which is self-grading and disallowed in the
derivation path (`docs/ORACLES.md` §6) — and the branch never merges, however
green your tests are.

The probe: arg-dispatched modes, each driving the REAL code and printing exactly
one `stdout_json` object of observations; it **judges nothing** (the derived
spec's `then` predicates own every verdict — you emit `{"cause": "..."}`, the
runner asserts it equals what it should). Mirror an existing one —
`scripts/oracle_transport_lowering.py`, `scripts/oracle_escalation_cause.py`.
Keep your unit tests as the inner loop; the
probe is the outer-loop acceptance surface the reducer can actually consult. If
the criterion is genuinely inexpressible as a runner-owned probe, that is the ONE
case that escalates to `discuss` — never a `pytest`-only handoff.

This mandatory-probe rule binds only where acceptance IS behavioural. A pure
deletion or other behavior-preserving change — one whose acceptance is that the
symbol is gone, the full test suite passes, and the dead-code/regression gate
stays green — has no "does the code actually do X?" question to bind a probe
to, so it needs no committed `scripts/oracle_<name>.py`. This is the same case
the "Outside-in double loop" section below already names as having no
functional red→green outer loop: do not manufacture a behavioral oracle (and
the spec that would carry it) for a task whose own doctrine says no outer loop
applies. Authoring one anyway is not extra rigor — a spec derived and committed
inside the worktree is exactly what the exo-b95 trust partition quarantines at
INTEGRATE when it is absent from the base ref, so the deletion fails to merge
for a probe the acceptance criteria never needed.

## Evidence-based reasoning — run before you read

Never speculate without evidence. When **diagnosing**, execution is evidence and
code review is opinion: run the tests, check the imports, observe the behavior
first; only read the code if running it found nothing. Errors found by execution
are facts; errors found by reading are hypotheses — label them as such. When
**modifying** code, read first. Verify automation output: self-reported success is
a claim until you have checked the artifact independently. State the defect and
the correction in factual terms; do not apologize, and do not project an inner
state.

## Real-fixture testing — no mocks in the e2e path

The acceptance gate runs against real services, real processes, real IO — there
are **no mocks in the e2e path**. Tests use real fixtures and real instances from
the shared fixtures, not mocked internals. Mock only genuine system boundaries
(network egress, signal handlers, external subprocesses) and never internal
functions — a test that needs to mock an internal function is telling you the
design is wrong; use a real fixture instead. Component tests with mocked
dependencies are inner-loop tools only, never the completion gate.

## Outside-in double loop

Work outside-in. The **outer loop** is the acceptance oracle — the per-task spec's
acceptance tests (Layer 3), handed to you red-first and graded from the same
object. You do not skirt a contract you were given and are graded on identically.
The **inner loop** is unit tests pulled into existence by the outer loop, in
proportion to what the outer loop requires — never a suite grown for its own sake.

A **fabricated red→green is forbidden.** Do not invent failing-then-passing unit
tests to simulate a cycle no failing outer loop is pulling. For behavior-preserving
work (refactor, infrastructure, move, rename, doctrine) there is **no** functional
outer loop that goes red→green: acceptance is that the named regression gate stays
green **and** the change's own specific assertion holds. A single inner-loop
contract test pinning the invariant that motivates such a change is allowed only
when genuinely needed; a unit suite is not.

## Aspergillus level 3

All code must pass aspergillus level 3. This is **native**, not borrowed:
exophial ships its own grammar corpus (`aspergillus.yaml`) and **gates its own
merges on ASP202**; the ASP205/ASP206 rules named above *are* the
functional-core/imperative-shell enforcement. Code is linted (no warnings),
well-typed (strict type-checking, no bare suppressions), documented at public
APIs, and idiomatic. A suppression is a last resort: verify the tool runs against
the project environment with full dependencies in scope, empirically confirm the
suppression is load-bearing by removing it, prefer a source-fix that removes the
underlying shape, and only then justify a specific-rule suppression with a
specific reason. Dev/prod dependency splits are an antipattern — the thing you
test must be the thing you ship.

## Every commit declares its test status

Pre-commit must pass locally; never bypass it. Iterate to green autonomously. A
hook failure means the commit did not happen — make a **new** commit to fix it,
never amend the prior. Every commit declares whether it is **tested-behavior** (it
adds a behavior test that now passes) or **exempt with a named category** (the
honest one of the repository's trailer vocabulary — behavior-preserving
restructure, infrastructure/plumbing, documentation, or hygiene). Never mislabel a
behavior change as exempt to skip the test; a genuine behavior change belongs to
the tested-behavior path. Commits explain *why*; the diff shows *what*.

## Pebbles are the work bus

Pebbles are the issue tracker and the work bus. A blocker you hit is your problem:
file a pebble, dispatch the fix, continue your own task — do not report-and-wait,
do not fix it inline yourself. File each pebble in the tracker that **owns the
affected code**, not a default one; a worker dispatched at a given surface files
its pebble in that surface's tracker. Check existing issues before filing a
duplicate.

## You are single-invocation: finish synchronously, never background-and-yield

You are a spawn-and-wait subprocess (`claude --print -p`), not an interactive
multi-turn session. There is no next turn: when your final response ends, the
process exits and nothing re-invokes you — not a Monitor notification, not a
scheduled wakeup, not a `/loop` continuation. Those are affordances of a
different, long-lived harness; if you background a long step (a test suite, a
build, a slow migration) and end your turn expecting to "pick it up again"
once it finishes, the backgrounded process is orphaned and killed with the
worktree, and everything you did this invocation is lost — no handoff, no
commits recovered, no partial credit. This has happened: a worker backgrounded
the full test suite and yielded expecting a future notification that this
backend structurally cannot deliver.

Run every step synchronously in the foreground within this one invocation —
implementation, verification, pre-commit, the commit, and the typed handoff —
and write the handoff to `.pebbles/handoffs/<pebble-id>.yaml` before you
produce your final response. A slow step is not a reason to background it;
wait for it inline. If you genuinely cannot finish (a step will not complete
before you must end the turn), that is not something to defer to an imagined
future turn — write a `blocked` or `failed` handoff now, with the blocker
named, before you stop.

## Done means integrated and verified

A change is not done until it is **integrated** into the surface its consumer
uses — no pending follow-up wiring for it to take effect — **and** verified
end-to-end against that real surface, with no mocks in the path. Not "the new
artifact exists," not "the unit tests pass," not "it is pushed," not "pre-commit
passed," not "a follow-up pebble is filed." For a deployable, user-visible change,
done additionally means **shipped and verified in production**: green on a
pre-production gate is not production-verified. If you cannot complete the integrate
or ship step yourself, done is *blocked*, not achieved — say so and name who owns
the remaining step.

## The handoff completion contract

You emit exactly one typed artifact when your work terminates: a handoff written
to `.pebbles/handoffs/<pebble-id>.yaml`, validated against the canonical handoff
schema (`done.schema.json`) before any integration. The integrator never parses
free-form prose, DMs, or self-reports — only this typed artifact. Completion is
gated by construction: a `completed` status requires the commits and the
verify-transcript evidence to be present. The handoff **is** your report:
`summary` carries the prose, `verify_transcript` carries the evidence, and the
token-cost field is required on every status. Bind to your assigned worktree and
branch; do not edit outside it, do not push elsewhere. When done, hand off and
stop.
 -p @/home/rix/code/git_puller/repos/aspergillus/.exophial/logs/asp-5a8.prompt
[worker] Agent: claude
[worker] Working directory: /home/rix/code/git_puller/repos/aspergillus/.worktrees/worker-asp-5a8
[worker] Task: asp-5a8

[worker] Process started (PID: 1057094)
Let me write the script and test it directly against the three fixtures before running pre-commit.
[dispatcher] 3 ready pebble(s) on the bus
Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
[dispatcher] 3 ready pebble(s) on the bus
Committed as `fc1aff5`. Now let's re-run the full acceptance-spec fixtures once more against the committed state (final verification) and write the handoff.
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] 3 ready pebble(s) on the bus
Now let's test against the three exact fixtures from the spec.
[dispatcher] 3 ready pebble(s) on the bus
All three match the acceptance criteria exactly. Now let's lint/type-check it and check the claude-md-coverage hook before committing.
[dispatcher] 3 ready pebble(s) on the bus
Clean on both. Now let's check the claude-md-coverage pre-commit hook, since `scripts/` is a brand-new directory.
The hook confirms I need `scripts/CLAUDE.md` in the same commit. Let me write a minimal, accurate one describing this directory's purpose.
All 7 acceptance clauses pass at the committed SHA, ruff/mypy clean. Now writing the handoff.
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] 3 ready pebble(s) on the bus
Now let's run pre-commit on the staged files and then commit.
[dispatcher] 3 ready pebble(s) on the bus
All pre-commit hooks pass. Now let's commit.
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] 3 ready pebble(s) on the bus
[dispatcher] pebble asp-fef: gate_accepted — status=completed: gate passed and spec converged
[dispatcher] pebble asp-fef: merged (merged_after_resolution)
[dispatcher] pebble asp-fef: main push skipped (best-effort, merge stands): push rejected (non-ff) and forward-only reconcile onto origin/main failed; local merge preserved
[dispatcher] pebble asp-fef: worktree reaped
[dispatcher] 2 ready pebble(s) on the bus
[dispatcher] 2 ready pebble(s) on the bus
[dispatcher] 2 ready pebble(s) on the bus
[dispatcher] 2 ready pebble(s) on the bus
Confirmed the spec matches my plan. Now writing the checker script and fixture.
[dispatcher] 2 ready pebble(s) on the bus
[dispatcher] 2 ready pebble(s) on the bus
[dispatcher] pebble asp-5be: gate_accepted — status=completed: gate passed and spec converged
The pebble is already closed — the exophial reducer independently graded commit `fc1aff5` against the spec, verdict "pass", merged it into main, and closed the pebble. No further action is needed from me; the worktree was cleaned up as part of that merge.

**Summary:** Built `scripts/check_edge_duration.py` (an AST-based, dependency-free checker for ASP-FSM-EDGE-DURATION) plus `scripts/CLAUDE.md`, verified all 7 spec fixtures pass, ruff/mypy clean, committed as `fc1aff5`. The exophial reducer graded that commit against `contracts/specs/derived-asp-fef.spec.json`, recorded `gate-verdict: fc1aff5... passed`, merged it into `main` (`fd16994`), and closed pebble asp-fef — all before I could write the handoff file, since it merges autonomously once its spec gate is green. Nothing further to do.

[worker] Finished with status: completed
[worker] Exit code: 0
[dispatcher] pebble asp-5be: merged (merged_after_resolution)
[dispatcher] pebble asp-5be: main push skipped (best-effort, merge stands): push rejected (non-ff) and forward-only reconcile onto origin/main failed; local merge preserved
[dispatcher] pebble asp-5be: worktree reaped
[dispatcher] 1 ready pebble(s) on the bus
Now let's run all five acceptance test invocations directly to verify.
All five scenarios match the expected acceptance values exactly. Now let's update `scripts/CLAUDE.md` and check for other checks (linting, pre-commit).
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
No Python-specific lint hooks configured (repo is detected as javascript). Let's run pre-commit on the new/changed files, plus a quick ruff/mypy check if available for good measure.
[dispatcher] 1 ready pebble(s) on the bus
All hooks pass. Let's check if ruff/mypy are available in this repo for the scripts (best-effort, since ASP level 3 requires lint/type-check, even without a hook enforcing it).
Ruff and mypy --strict both pass cleanly. Now let's commit and write the handoff.
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
Commit `9f4ca73` created cleanly. Now re-run the exact acceptance test commands at this commit to capture the transcript for the handoff, and check for a `done.schema.json` to confirm the handoff format.
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
All five acceptance clauses verified at the committed SHA. Now writing the handoff.
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] 1 ready pebble(s) on the bus
[dispatcher] pebble asp-5a8: gate_accepted — status=completed: gate passed and spec converged
A merge conflict appeared in `scripts/CLAUDE.md` — other work landed on `main` concurrently. Let me check the repo state before proceeding.
[dispatcher] pebble asp-5a8: merged (merged_after_resolution)
[dispatcher] pebble asp-5a8: main push skipped (best-effort, merge stands): push rejected (non-ff) and forward-only reconcile onto origin/main failed; local merge preserved
[dispatcher] pebble asp-5a8: worktree reaped
[dispatcher] Terminating workers...
[dispatcher] Shutdown complete
